# Keep in sync with version.json's "version" field at the repo root — a
# git pull changes this; version.json is what a release publishes for the
# on-demand update check to compare against.
__version__ = "0.1.0"
